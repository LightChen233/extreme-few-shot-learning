# Feature Agent Prompt

你是特征工程专家。

## 任务
{task_description}

## 当前性能
验证损失: {val_loss:.4f}

## 各样本预测误差（正值=低估，负值=高估，按总误差降序）
{val_errors}

重点关注误差最大的工艺条件，思考其背后的物理原因，针对性设计特征。

## 领域知识
{domain_knowledge}

## 历史反思
{reflection_context}

## 当前代码
```python
{current_code}
```

## 要求

### 第一步：DOMAIN_PARAMS（基于你的世界知识）
在代码开头定义，包含你从领域知识中推断的关键物理参数和机制边界，例如：
```python
DOMAIN_PARAMS = {{
    # 根据你的知识填写，不要硬猜，要有物理依据
    'key_threshold_1': value,  # 说明物理意义
    ...
}}
```

### 第二步：physics_baseline(temp, time)
实现一个基于世界知识的基线预测函数，返回 (baseline_strain, baseline_tensile, baseline_yield)。
这个函数应基于已知的物理规律（如 Arrhenius、时效曲线趋势等）给出近似预测。

```python
def physics_baseline(self, temp, time):
    # 基于领域知识的基线估计（不依赖训练数据标签）
    ...
    return baseline_strain, baseline_tensile, baseline_yield
```

### 第三步：engineer_features(df)
在特征中包含：
- 相对基线的残差特征（如 `temp_relative_to_regime_boundary`）
- 物理机制区间检测特征（基于 DOMAIN_PARAMS 中的阈值）
- 基线预测值本身作为特征（让模型学残差）
- 其他动态推理特征

直接输出完整代码。
