# Feature Agent Prompt

你是特征工程专家。

## 任务
{task_description}

## 当前性能
验证损失: {val_loss:.4f}

## 各样本预测误差（正值=低估，负值=高估，按总误差降序）
{val_errors}

重点关注误差最大的工艺条件，思考其背后的物理原因，针对性设计特征。

## 数据覆盖分析
{coverage_analysis}

重点：对于**外推点**（训练集无该条件），应设计能反映"距训练集边界距离"的特征，或引入物理先验约束外推方向，而不是依赖数据插值。

## 数据规模感知
训练样本数：**{n_train} 条**

根据这个数字判断特征维度的合理上限：
- 样本极少（<50）时，特征数过多（>样本数）会导致严重过拟合，下游模型无法泛化
- 此时应优先选择**少而精**的物理特征，而非堆砌所有可能的派生特征
- 判断逻辑由你自己根据数据量决定，不强制限制

## 领域知识
{domain_knowledge}

## 历史反思
{reflection_context}

**重要提示**：如果历史反思显示"所有改动都使性能变差（✗）"，说明当前特征工程已经是相对最优的。此时你应该：
1. 只做**极小幅度**的调整（如微调阈值参数、增减1-2个特征），不要重写整个特征集
2. 特别注意：增加过多特征在小样本下会导致过拟合，反而变差
3. 如果不确定，宁可保持当前特征不变，只微调 DOMAIN_PARAMS 中的数值

## 当前代码
```python
{current_code}
```

## 要求

### 第一步：DOMAIN_PARAMS（基于你的世界知识）
在代码开头定义，包含你从领域知识中推断的关键物理参数、动力学参数和**机制边界（Phase/Regime Boundaries）**。特别注意：很多物理过程存在明显的突变或转折点（如相变、过时效、析出粗化等）。
```python
DOMAIN_PARAMS = {{
    # 必须包含可能的临界点或阈值（例如临界温度、临界时间或其组合）
    'critical_temp_regime_shift': value,  # 发生机制突变的温度点
    'activation_energy_Q': value,         # 动力学激活能估计
    # 其他根据你的知识推断的关键阈值
}}
```

### 第二步：physics_baseline(temp, time)
实现一个基于世界知识的基线预测函数，返回 (baseline_strain, baseline_tensile, baseline_yield)。
这个函数应基于已知的物理规律给出近似预测。

**关键约束：physics_baseline 的预测方向必须与数据统计一致。**
在实现前，先从领域知识中确认：在当前温度范围内，更高温度/更长时间对应更高还是更低的强度？
不要凭空假设"过时效"或"软化"，除非数据中有明确的非单调趋势。

```python
def physics_baseline(self, temp, time):
    # 基于领域知识的基线估计（不依赖训练数据标签）
    ...
    return baseline_strain, baseline_tensile, baseline_yield
```

### 第三步：engineer_features(df)
在特征中包含：
- **物理机制区间检测特征（关键）**：基于 DOMAIN_PARAMS 中的阈值，构建**布尔特征或分段激活特征**（例如 `is_overaged = (temp >= T_c) & (time >= t_c)`），帮助下游模型捕捉性能的阶跃和突变。
- **动力学等效特征**：构建如 Larson-Miller Parameter (LMP = T*(C + log(t))) 或 Arrhenius 指数项，将多维工艺参数映射到反映物理进程的一维尺度上。
- 基线预测值本身作为特征（让模型学残差）
- 相对基线的残差特征（如 `temp_relative_to_regime_boundary`）

直接输出完整代码。
