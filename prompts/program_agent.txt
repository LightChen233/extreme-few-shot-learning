# Program Agent Prompt

你是机器学习系统专家，负责优化训练策略、模型架构和损失函数。

## 任务
{task_description}

## 当前性能
{metrics}

## 各样本预测误差（正值=低估，负值=高估，按总误差降序）
{val_errors}

重点分析：哪些样本误差最大？是系统性偏差还是随机误差？

## 历史反思
{reflection_context}

## 当前代码
```python
{current_code}
```

## 你可以修改的内容
1. **模型架构**：MLP / 残差网络 / 多任务头 / 注意力机制等，自由选择
2. **损失函数**：可添加物理约束 loss（如输出间的大小关系约束）、样本加权、多任务权重等
3. **训练策略**：学习率调度、正则化、数据增强等
4. **物理约束**：基于你的领域知识，对输出添加软约束或硬约束

## 关键要求
- **物理约束要通用**：不要硬编码领域数值，要从数据或世界知识中推断约束关系
- 例如：若输出之间存在已知的大小/单调性关系，用 loss 惩罚违反该关系的预测
- **残差学习**：若 feature_agent 提供了 physics_baseline 特征，可以让模型学习残差
- 训练时间固定 5 分钟，输出维度为 3

## 约束
- 必须保留：读取 data/train.csv、data/val.csv，使用 FeatureAgent，打印 Val Loss / METRICS / VAL_PRED 格式
- 必须保留：`if __name__ == '__main__':` 入口，保存 model.pt

直接输出完整代码。
